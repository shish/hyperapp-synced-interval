{
  "name": "@shish2k/hyperapp-synced-interval",
  "version": "0.0.1",
  "description": "Call a function on the second, every second, synced across clients",
  "keywords": [
    "hyperapp",
    "interval"
  ],
  "main": "dist/index.js",
  "author": "Shish <shish+npm@shishnet.org>",
  "license": "MIT",
  "repository": "shish/hyperapp-synced-interval",
  "homepage": "https://github.com/shish/hyperapp-synced-interval",
  "bugs": {
    "url": "https://github.com/shish/hyperapp-synced-interval/issues"
  },
  "peerDependencies": {
    "hyperapp": "^2.0.0"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.20.2",
    "jest": "^29.3.0",
    "jest-mock-fetch": "^2.0.5",
    "parcel": "^2.7.0",
    "prettier": "^2.7.1"
  },
  "scripts": {
    "clean": "npx rimraf coverage dist node_modules .parcel-cache",
    "format": "prettier --write '{src,test}/**/*.js'",
    "test": "jest --coverage --no-cache",
    "build": "parcel build src/index.js",
    "prepare": "npm run format && npm test && npm run build",
    "release": "./pre-flight-tests && npm run clean && npm i && ./pre-flight-tests && git tag $npm_package_version && git push && git push --tags && npm publish --tag latest --access public"
  },
  "prettier": {
    "semi": true
  },
  "babel": {
    "presets": [
      "@babel/env"
    ]
  },
  "jest": {
    "automock": false,
    "setupFiles": [
      "./test/setupJest.js"
    ]
  }
}
